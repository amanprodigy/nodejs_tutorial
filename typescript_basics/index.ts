import {Person} from './classes';
import * as _ from 'lodash';

var person = new Person('Aman', 'Srivastava');
console.log(person);

var arr = [1, 3, 5, 7, 9];
var newArr = _.reverse(arr);
console.log(newArr);

